<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Image -->
    <style name="imageView" parent="mainWrapContent">
        <item name="android:scaleType">fitCenter</item>
        <item name="android:adjustViewBounds">true</item>
    </style>

    <style name="mainMatchParent">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="mainWrapContent">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="mainView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="textQuestion" parent="mainView">
        <item name="android:textSize">@dimen/textSize1</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textStyle">bold</item>
        <item name="android:padding">@dimen/padding2</item>

    </style>

    <style name="textAnswer" parent="mainView">
    <item name="android:textSize">@dimen/textSize1</item>
    <item name="android:fontFamily">sans-serif</item>
    <item name="android:textColor">@color/black</item>
    <item name="android:padding">@dimen/padding2</item>
</style>

    <style name="textTitle" parent="mainView">
        <item name="android:textSize">@dimen/textSize2</item>
        <item name="android:fontFamily">sans-serif-condensed</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:padding">@dimen/padding1</item>
        <item name="android:gravity">center</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="layout" parent="mainView">
        <item name="android:layout_marginRight">@dimen/margin2</item>
        <item name="android:layout_marginLeft">@dimen/margin2</item>
        <item name="android:layout_marginTop">@dimen/margin2</item>
        <item name="android:orientation">vertical</item>
    </style>

    <style name="button" parent="mainWrapContent">
        <item name="android:layout_marginRight">@dimen/margin2</item>
        <item name="android:layout_marginLeft">@dimen/margin2</item>
        <item name="android:layout_marginTop">@dimen/margin2</item>
        <item name="android:orientation">vertical</item>
        <item name="android:layout_centerVertical">true</item>
    </style>

</resources>
